# 双机协作规范（足球分析）

> 制定日期：2026-08-13
> 适用：老大（台式 TSE2021 / xieyu，当前 agent 运行环境）＋ 老二（平板 Surface Pro 7 / TABLET-7LKSMOUL）
> 目的：让双机在分析、同步、容灾上有一份单一真相源，避免编号混乱、同步覆盖、数据丢失。

---

## 1. 双机物理身份与角色

| 机器 | 身份 | 物理特性 | 角色定位 |
|------|------|----------|----------|
| 台式 TSE2021（xieyu） | 老大 | 长期开机、算力强 | **常驻主脑**：重计算、数据沉淀、定时任务、最终拍板、编号权 |
| 平板 TABLET-7LKSMOUL（Surface Pro 7） | 老二 | 便携、随身带出门、四核可跑重模型 | **移动主脑**：看报告、录临场情报、轻交互；台式挂掉时可无缝接管完整分析 |

**关键认知**：双机都是满血战力，不是「大脑＋弱终端」。平时一台干、一台待命；谁挂了另一台直接接管。

---

## 2. 编号体系（单一真相源）

- **引擎层 `#N`**：铁律引擎唯一编号，`#1-43` 已闭环（含 `#43 数据缺口零容忍`）。
- **流程层 `P1-P7`**：流程步骤独立编号，不占用引擎编号。
- **引用格式**：引擎用 `#N`，流程用 `P-N`。禁止混用、禁止单边私自加/改编号。
- **新增规则**：从 `#44` 起，必须双机协商一致后再入库，并同步更新《规则编号映射表》。
- **冲突教训**：曾出现 Rule 4/9/26 两边名称对不上，根因是「编号 vs #编号」口径分裂。以引擎 `#N` 为唯一权威。

---

## 3. 同步纪律（git-sync）

脚本：`C:/Users/xieyu/.workbuddy/skills/git-sync.py`（双机均已为修复版，含 do_push 逐文件 upsert＋base64＋限流重试、do_pull 逐文件隔离＋size 校验＋重试）。

- **开工前先 `pull`**：避免用旧基线覆盖对方工作（老二 v7.5.2.14 曾被老大旧提交覆盖的教训）。
- **定时自动 push（不只是 pull）**：台式设每 30–60 分钟自动 `git-sync push`，保证云端永远有最新基线，关机也不丢。
- **出门前手动确认 push 完成**再带平板走。
- **严禁双机同时跑分析**：会产生冲突文件（Syncthing/MEMORY 铁律）。切换时必须先确认另一台已停。
- **大文件不再漏**：lessons.md（480KB）等经 size 校验＋重试，pull/push 均不会静默跳过。
- **push 后提醒对方 pull**：别只凭「我推了」就当对方有了。

---

## 4. 信箱异步通信规范

- 通道：WorkBuddy 空间文档（node `oyLescekNmZkp1ODmBQ4Bv`）。
- 换 token：`connect_open_platform(skill_id=library)`。
- 读：`get_doc_reviews.py --page-id`；写：`submit_doc_edit.py --actions-file`（stdin 只给 token）。
- **组件语法铁律**：content 必须用大写组件标签（`<Paragraph>…</Paragraph>`、`<Heading>…</Heading>`），**禁止裸 Markdown/纯文本**，否则报「必须使用组件语法」。
- **引号陷阱**：content 内用英文双引号会破坏 JSON，改用中文引号或转义。
- 所有跨机信息走信箱，不依赖直接文件互拷。信箱是永不掉线的中立通道。

---

## 5. 日常流程：10 场截图 SOP

### 场景 A：在家（用户直接给台式/老大截图）
1. 截图落盘 `D:/1/Claw/8-13_十场竞彩.png`，命名带日期。
2. 老大先 `git-sync pull` 保最新。
3. 老大识别 10 场（队名/联赛/时间/竞彩编号），列任务清单。
4. 老大定分析深度：先初筛，挑 3–4 场深研，其余给方向性结论。
5. 老大派单给老二（信箱）：场次清单＋要求。
6. 老二拉 6 维情报（伤停/战意/近期状态/H2H/盘口变化/阵容）＋ 四猿量化（Elo＋泊松＋蒙特卡洛）产草稿。
7. 老二回草稿（信箱）。
8. 老大用 `iron_law_judge.py` 审计（重点 `#43 数据缺口零容忍`、铁律命中），拍板最终表。
9. 出表（主选/备选/概率%/防平·爆冷/串关建议）→ `git-sync push`。
10. 赛后双机回填 `lessons.md`。

### 场景 B：出门带平板（用户在外面拿到截图）
1. 平板拍下/收到截图 → 老二 `pull` 拿当日已有产物。
2. 老二轻量初筛＋录现场情报 → 信箱回传台式。
3. 台式（长期开机）收到 → 跑重计算深化 → `push`。
4. 平板再 `pull` 看最终表，现场决策。

---

## 6. 容灾降级（台式关机/断网）

数据三副本，永不单点：①台式本地 ②GitHub 云端（push 过）③信箱云端。

- **断网（台式还开着）**：本地分析照常；联网活儿（赔率/初筛）暂停；网络恢复后补 push＋发信箱。
- **台式关机/宕机**：平板（Surface Pro 7）直接 `pull` 拿最新基线 → 单核起飞跑完整分析（量化＋铁律审计＋出表），无缝接管。
- **切换纪律**：正常不同时跑（防冲突）；台式确认挂了才切平板；平板切之前先 `pull` 保证数据最新；台式恢复后先确认平板已停再开。
- **临场情报**：台式挂时先存信箱，恢复后补处理。

---

## 7. 数据资产沉淀（长期最值钱）

- **`lessons.md` 复盘库**：每场赛后回填实际赛果 vs 预测、铁律命中/失效。已 480KB，必须完整同步（已修大文件坑）。
- **球队画像档案库**：触发标签调整的逻辑固化，别每场从零查。
- **纪律**：每周/每轮联赛后必有复盘写入；赛果回流统计铁律命中率、爆冷预警成功率、防平准确率，用真实数据反向校准阈值。
- **防退化**：`#43 数据缺口零容忍` 已立，禁止「为下单而凑情报」绕过门禁——缺口大就降星/弃投。

---

## 8. 一句话总纲

**两台都是满血主脑：台式 7×24 常驻主脑，平板随时接管移动主脑。平时一台干一台待命，谁挂了另一台直接起飞；数据靠 GitHub＋信箱双云端不丢；编号以 `#N` 为唯一权威；所有跨机信息走信箱。**
