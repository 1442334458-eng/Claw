# 足球竞彩分析项目 — 长期优化方案 v2.2

> 生成时间：2026-08-11 | 版本：v2.2 | 基于当前架构审计
> 目标：面向长期运营的架构加固、工作流精简、质量持续提升

---

## 一、现状诊断（8月11日节点）

### 已完成的基础设施

| 模块 | 状态 | 说明 |
|:---|:---:|:---|
| 管道 v2.2 六层数据采集 | ✅ | 伤病/H2H/赔率/PFI/天气/新闻 |
| 多源赔率 三级降级 | ✅ | TheOddsAPI → 500.com/竞彩 → WebSearch |
| 天气 Open-Meteo 直连 | ✅ | 50+ 球队城市自动映射 |
| 铁律 v7.5.2.7 (29条) | ✅ | DOIT + 赔率 + 盘口 + 基本面 + 情报 + 反常 |
| 三猿闭环 (情报→分析→管家) | ✅ | Phase 1/2/3 走通 |
| 双机同步 (xieyu ↔ Surface) | ✅ | SSH Git + Issue信箱 + 自动检查 |
| v2.1 采集器 (球迷屋+FotMob) | ✅ | 已接入 Layer 1/2/4 |
| v2.2 澳彩 Phase2 | ✅ | 多源赔率采集器集成 Layer 3 |

### 当前痛点

| 痛点 | 严重度 | 根因 |
|:---|:---:|:---|
| The Odds API 月配额 500 不够用 | 🔴 高 | 月配额固定，无法扩展 |
| 铁律 29 条需要 Agent 手动跑 | 🟡 中 | 脚本只输出任务清单，缺少自动裁决 |
| 复盘循环未自动化 | 🟡 中 | Phase 4 复盘猿手动触发 |
| 多副本代码散落 | 🟡 中 | data-pipeline/ vs v8/ 两套代码 |
| 输入依赖截图 | 🟠 低 | 截图→OCR→解析链路长 |

---

## 二、推荐优化路线图

### Phase A: 立即做（本周，≤2小时）

#### A1. 清理代码重复

```
当前冗余:
  data-pipeline/multi_source_odds.py  (759行, 旧版)
  football-pipeline-v8/multi_source_odds.py  (1081行, v1.1)
  football-pipeline-v8/football-pipeline-v8/multi_source_odds.py  (1081行, 完全重复)

建议:
  保留: football-pipeline-v8/multi_source_odds.py (主版本, v1.1)
  归档: data-pipeline/multi_source_odds.py → data-pipeline/_archived/
  删除: football-pipeline-v8/football-pipeline-v8/ 整个重复子目录
```

#### A2. 管线一键运行脚本

创建 `D:\1\Claw\.workbuddy\skills\football-pipeline-v8\run_pipeline.bat`：

```bat
@echo off
chcp 65001 >nul
cd /d "%~dp0"
set PYTHON=%USERPROFILE%\.workbuddy\binaries\python\versions\3.13.12\python.exe

echo ========================================
echo   足球管道 v2.2 — 一键运行
echo ========================================
echo.

REM 读取比赛文件
set MATCH_FILE=matches_%date:~0,4%%date:~5,2%%date:~8,2%.txt
if not exist "%MATCH_FILE%" set MATCH_FILE=MATCHES_TEMPLATE.txt

echo [1/3] 运行数据管道 (6层采集)...
%PYTHON% -u main.py --file "%MATCH_FILE%" --output report_raw.json 2>&1 | tee pipeline_log.txt

echo [2/3] 生成铁律报告...
%PYTHON% -u build_report_v82_fast.py --input report_raw.json --output "..\..\..\..\铁律报告_%date:~0,10%.md"

echo [3/3] 推送到 GitHub...
cd /d "%~dp0" && git add -A && git commit -m "auto: pipeline run %date%" && git push

echo.
echo ========================================
echo   完成! 报告已生成
echo ========================================
pause
```

#### A3. 复盘自动化模板

在 `football-pipeline-v8/review_template.py` 新增：

```python
# 赛后复盘数据收集
def collect_match_results(match_date: str) -> List[Dict]:
    """从竞彩API/500.com收集实际比分"""
    ...

def compute_hit_rate(predictions: List[Dict], results: List[Dict]) -> Dict:
    """计算命中率、ROI、各星级准确率"""
    ...
```

---

### Phase B: 本周内做（1-2天）

#### B1. DOIT + 铁律裁决脚本化

当前：Agent 手动逐场过 29 条铁律 → 效率低、容易漏。

建议：写 `iron_law_judge.py`，输入 `cache/*.json` 文件，自动输出裁决结果。

```python
# iron_law_judge.py 核心逻辑
class IronLawJudge:
    """铁律自动裁决器"""

    def judge_match(self, match_data: MatchData) -> IronLawVerdict:
        verdict = IronLawVerdict()
        verdict.base_stars = self._doit_score(match_data)

        # 逐条判定（全部自动化）
        for rule in range(1, 30):
            result = self._apply_rule(rule, match_data)
            verdict.rules[rule] = result

        verdict.final_stars = verdict.base_stars + verdict.bonus_stars
        return verdict

    def _apply_rule(self, rule_num: int, data: MatchData) -> RuleResult:
        """单条铁律判定（数据驱动，不依赖Agent主观判断）"""
        rules = {
            1: self._rule_kelly_sink,
            2: self._rule_return_rate,
            ...
            29: self._rule_starting_lineup_leak,
        }
        return rules[rule_num](data)
```

#### B2. 赔率配额优化：500.com 直连替代

现状：TheOddsAPI 500/月 → 配额紧张。

方案：**500.com 作为主赔率源**（免费、无限量、含亚盘水位）。

```
实现路径:
  1. multi_source_odds.py 的 SportteryFetcher 已有竞彩官方API调用逻辑
  2. FiveHundredFetcher 已有500.com HTML解析逻辑
  3. 调整 Layer 3 优先级:
     新: 500.com(主) → 竞彩官方(验证) → TheOddsAPI(补充) → WebSearch

  预期效果:
    - 日均可分析 50+ 场（不受API配额限制）
    - 亚盘水位数据完整（500.com 含49家公司）
    - TheOddsAPI 仅用于 Pinnacle/Bet365 国际赔率验证（用量降80%）
```

#### B3. 胜率追踪仪表盘

在 `tracking.json` 持续记录：

```json
{
  "total_bets": 0,
  "total_won": 0,
  "by_star": {
    "5": {"bets": 0, "won": 0},
    "4": {"bets": 0, "won": 0},
    "3": {"bets": 0, "won": 0}
  },
  "by_type": {
    "single": {"bets": 0, "won": 0},
    "2x1": {"bets": 0, "won": 0},
    "3x1": {"bets": 0, "won": 0}
  },
  "roi_total": 0.0,
  "roi_by_month": {
    "2026-08": 0.0
  }
}
```

赛后自动更新 + 每周生成胜率报告。

---

### Phase C: 本月内做（1-2周）

#### C1. 竞彩官网 API 直连 (SportteryFetcher 激活)

`multi_source_odds.py` 已有 `SportteryFetcher` 类（含 `get_match_list`、`to_source_odds`），但当前只生成了框架代码，实际 HTTP 调用未写。

```python
# 补充实际的API调用
def get_match_list(self, match_date: str = None) -> List[Dict]:
    url = "https://webapi.sporttery.cn/gateway/lottery/getFootballMatchListV1.qry"
    params = {"matchDate": match_date or datetime.now().strftime("%Y-%m-%d")}
    resp = requests.get(url, params=params, headers=HEADERS)
    data = resp.json()
    return data.get("value", {}).get("matchList", [])
```

#### C2. 比赛输入全自动化

```
当前流程: 截图 → Agent OCR解析 → 手动确认
目标流程: 每日 8:00 自动跑

方案:
  1. SportteryFetcher.get_match_list() 获取当日全部竞彩场次
  2. 自动生成 matches_YYYYMMDD.txt
  3. 触发生成管道
```

#### C3. 日报自动推送

```
产出:
  - 分析报告 (Markdown) → 本地 + GitHub
  - 串关摘要 (文本) → 可复制到手机
  - 胜率追踪更新 (JSON)

触发: 每日 10:00 自动运行 (或手动 bat)
```

---

### Phase D: 长期迭代（1-3个月）

#### D1. 模型校准闭环

```
赛后收集:
  1. 实际比分 → 验证 SPF/让球/大小球命中
  2. 每场铁律判定与实际结果的偏差
  3. 各星级准确率月度趋势

校准动作:
  1. 调整 DOIT 权重 (连续偏差异常→自动微调)
  2. 调整铁律加减星幅度 (基于回测数据)
  3. 更新 league_map 优先级 (联赛特性积累)
```

#### D2. 回测框架

```python
# backtest.py
class BacktestEngine:
    def run(self, start_date: str, end_date: str):
        """对历史日期批量跑管道 → 对比实际结果 → 计算各项指标"""
        for date in date_range(start_date, end_date):
            matches = self.fetch_historical_matches(date)
            predictions = self.pipeline.run(matches)
            results = self.fetch_historical_results(date)
            self.record(predictions, results)
        return self.statistics()
```

#### D3. 知识库积累

```
lessons.md 结构化改进:
  格式: | 日期 | 对阵 | 预测方向 | 实际结果 | 偏差原因分类 | 经验教训 |
  用途: 1) 偏差模式识别 2) 联赛特性积累 3) 铁律调整依据
```

---

## 三、架构优化建议

### 3.1 文件结构收拾

```
当前: 散落三处
  D:\1\Claw\data-pipeline\          (Syncthing共享, 旧版)
  D:\1\Claw\.workbuddy\skills\football-pipeline-v8\  (v8仓库, 最新)
  D:\1\Claw\.workbuddy\skills\football-pipeline-v8\football-pipeline-v8\  (重复子目录)

建议合并:
  D:\1\Claw\.workbuddy\skills\football-pipeline-v8\  ← 主仓库 (权威)
  ├── main.py                  (管道主脚本)
  ├── multi_source_odds.py     (多源赔率采集)
  ├── qiumiwu_collector.py     (球迷屋采集器)
  ├── fotmob_collector.py      (FotMob采集器)
  ├── iron_law_judge.py        (铁律自动裁决, 新增)
  ├── build_report_v82.py      (报告生成 v8.2)
  ├── cross_validate.py        (赔率交叉验证)
  ├── scheduler.py             (定时任务)
  ├── review_template.py       (复盘模板, 新增)
  ├── backtest.py              (回测引擎, 新增)
  ├── tracking.json            (胜率追踪, 新增)
  ├── MATCHES_TEMPLATE.txt     (比赛输入模板)
  ├── cache/                   (缓存目录)
  ├── logs/                    (日志目录)
  └── output/                  (报告输出目录, 新增)

  D:\1\Claw\data-pipeline\_archived\  ← 归档旧版 (不再维护)
```

### 3.2 数据流优化

```
当前流程 (重依赖Agent):
  截图 → Agent OCR → Agent WebSearch → Agent WebFetch → Agent 铁律裁决 → 输出报告
  问题: Agent 上下文窗口有限，多场次容易出错/遗漏

建议流程 (脚本化+Agent辅助):
  定时 bat → 管道脚本 (自动6层采集) → 铁律脚本 (自动29条裁决)
           → Agent (仅深度分析 ≥4星球场) → 管家脚本 (自动串关+仓位+报告)
  
  Agent 参与范围缩小: 只做深度解读，不再逐场跑铁律
```

### 3.3 Skill 更新

`football-pipeline-v8/SKILL.md` 需同步更新到 v2.2，关键变更：

```markdown
## v2.2 变更 (2026-08-11)
- Phase2: 多源赔率引擎 (500.com/竞彩官方/澳彩)
- Layer 3 三级降级链: TheOddsAPI → MultiOdds → WebSearch
- Layer 5 天气自动推断: TEAM_CITY_MAP + Open-Meteo 直连
- 新增: MATCHES_TEMPLATE.txt 标准输入格式
- 新增: 澳彩亚盘水位数据源
```

---

## 四、优先级总览

| 优先级 | 项目 | 耗时 | 产出 |
|:---:|:---|:---:|:---|
| 🔴 P0 | 批量铁律裁决脚本化 | 4h | 不再手动逐场过29条 |
| 🔴 P0 | 500.com 替代 TheOdds 做主源 | 2h | 赔率不再受配额限制 |
| 🔴 P0 | 清理重复代码子目录 | 0.5h | 减少维护混乱 |
| 🟡 P1 | 胜率追踪 + 复盘模板 | 3h | 闭环质量积累 |
| 🟡 P1 | 竞彩API直连激活 | 2h | 比赛列表自动获取 |
| 🟡 P1 | 管线一键运行 bat | 0.5h | 工业化操作 |
| 🟢 P2 | 月度回测框架 | 8h | 模型校准依据 |
| 🟢 P2 | 日报自动推送 | 4h | 全自动化 |
| 🟢 P2 | lessons.md 结构化 | 2h | 知识积累 |

---

## 五、本日产出总结

| # | 修复/新增 | 文件 | 状态 |
|:---:|:---|:---|:---:|
| 1 | 串关方案升级 (凯利值+仓位+止损) | 铁律报告 | ✅ |
| 2 | 比赛输入模板标准化 | MATCHES_TEMPLATE.txt | ✅ |
| 3 | 澳彩 Phase2 集成 Layer 3 | main.py + multi_source_odds.py | ✅ |
| 4 | 长期优化方案 | 本文件 | ✅ |
| 5 | 版本升级 v2.1→v2.2 | main.py | ✅ |
