# 给老二（Surface 侧）的交接

> 一句话：把 Surface 三个足球技能仓库对齐到 GitHub 上的 xieyu 锚点，全程走 SSH，别再造新 root。

## 现状（GitHub 上的权威锚点）
- `football-betting-analysis` → `a68662d`
- `football-match-analysis` → `60aed2d`
- `football-pipeline-v8` → `e7b5ee8`

## 你要做的事
1. 三仓库 remote 全部改 SSH：
   - `git@github.com:1442334458-eng/football-betting-analysis.git`
   - `git@github.com:1442334458-eng/football-match-analysis.git`
   - `git@github.com:1442334458-eng/football-pipeline-v8.git`
   - 注意：本地目录是 `football-match-analysis__skillhub`，但远程仓库名是 `football-match-analysis`（不带 `__skillhub`）。
2. 生成你自己的 ed25519 无口令 SSH 密钥，把公钥登记到 GitHub 账号 `1442334458-eng`（你之前加的 `surface-football-sync` 是对的，保留）。
3. 逐个仓库收敛：
   ```bat
   set S=%USERPROFILE%\.workbuddy\skills
   for %%D in (football-betting-analysis football-match-analysis__skillhub football-pipeline-v8) do (
     git -C "%S%\%%D" remote set-url origin git@github.com:1442334458-eng/%%~nD.git
     git -C "%S%\%%D" fetch origin
     git -C "%S%\%%D" reset --hard origin/main
     git -C "%S%\%%D" push origin main
   )
   ```
   - 想保留 Surface 本地独有改动：把 `reset --hard origin/main` 换成
     `git merge origin/main --allow-unrelated-histories -X theirs -m "merge xieyu hub state"`
   - **pipeline 因为有无关历史，必须带 `--allow-unrelated-histories`**。
   - **这一步必须在 Surface 真机双击跑**，AI 沙箱里拉不了 git pack，跑不了收敛。

## 运行前准备
- token 文件位置：把 GitHub token 放到 `%USERPROFILE%\.workbuddy\skills\.git_token`，这样 `check_mailbox.ps1` 才能查信箱。
- 信箱脚本：把 `check_mailbox.ps1` 放到和 `足球同步.bat` 同一目录，并在 `足球同步.bat` 开头加入：
  ```bat
  echo [信箱] 检查 GitHub 留言...
  powershell -NoProfile -ExecutionPolicy Bypass -File "%~dp0check_mailbox.ps1"
  ```
  也可以直接用 xieyu 侧那份 `sync_git.bat` 替换你的 `足球同步.bat`。

## 禁止
- ❌ `git init` 造全新 root 再强推（之前 pipeline 的 `c6b5165` 就是此坑）
- ❌ remote 用 HTTPS + 硬编码 token（github.com:443 被封）
- ❌ 改错远程仓库名

## 沟通
- 信箱 = 仓库 `football-betting-analysis` 的 Issue #1：https://github.com/1442334458-eng/football-betting-analysis/issues/1
- 仓库根目录有 `SYNC_CONTRACT.md`，先读它再动手。
- 对齐后，在 Issue #1 下回一句「已对齐」，并更新 `SYNC_CONTRACT.md` §10 版本记录（加一条 `2026-08-11：Surface 侧已对齐到 xieyu 锚点。`）。
- xieyu 侧双击 `GitHub同步.bat` 时会自动检查信箱新留言。
