# 双机 GitHub 同步设置指南（路线 A：真 git 仓库 + 真合并）

> xieyu 与 Surface 都按本文操作即一致。本文是唯一真相源，改动后推到 GitHub，另一台 `git pull` 即可看到同一份。

---

## 一、核心机制（两台完全一致）

- 3 个足球技能 = 3 个**独立真 git 仓库**，各自对应一个 GitHub 仓库：
  - 本地目录 `football-betting-analysis` → 仓库 `football-betting-analysis`
  - 本地目录 `football-match-analysis__skillhub`（注意这串后缀）→ 仓库 `football-match-analysis`
  - 本地目录 `football-pipeline-v8` → 仓库 `football-pipeline-v8`
- **真合并**：日常流程 `git add -A → commit → pull → push`。git 自动三路合并；只有同一文件同一段落两边各改才会冲突（手动解）。
- **传输走 SSH（github.com:22）**：本机网络环境下 `github.com:443` 被封/间歇性超时，但 22 端口稳定可达，所以 remote 用 `git@github.com:...` 而非 HTTPS+token。
- **凭证 = SSH 密钥**：每台机器各生成一把 ed25519 密钥，公钥加进同一个 GitHub 账号（Settings → SSH and GPG keys）。
- **WorkBuddy 读技能 = 符号链接**：用户级 `skills` 目录里的技能名 → 指到技能源目录（见第二/三节）。
- 旧的 `git-sync.py`（API 覆盖式）作为**备用**保留，平时不再使用。

---

## 二、xieyu（老电脑）—— 已完成

- 技能源 = `D:\1\Claw\.workbuddy\skills\football-*`（这 3 个目录**本身就是 git 仓库**，含完整历史）
- SSH 密钥：`C:\Users\xieyu\.ssh\id_ed25519_claw`（公钥见第四节）
- 同步脚本：`D:\1\Claw\.workbuddy\skills\sync_git.bat`
- 桌面 `GitHub同步.bat` 已改为调用 `sync_git.bat`

> 唯一待办：把 xieyu 的公钥加到 GitHub（第四节），然后双击桌面 `GitHub同步.bat` 把 3 个仓库推上去。

---

## 三、Surface（新电脑）首次设置

1. **装 Git**：https://git-scm.com —— 默认选项一路下一步即可（装完重开终端）。
2. **确定技能源** `<SKILLS>` = `C:\Users\Surface\.workbuddy\skills`（技能直接住这里，无需克隆 Claw 仓库）。
3. **生成 SSH 密钥**：
   ```
   ssh-keygen -t ed25519 -C "claw-surface@github-sync" -N ""
   ```
   公钥在 `C:\Users\Surface\.ssh\id_ed25519.pub`，按第四节方法加进 GitHub。
4. **clone 3 个仓库**（也可直接运行本仓库的 `setup_surface_git.bat`）：
   ```
   cd C:\Users\Surface\.workbuddy\skills
   git clone git@github.com:1442334458-eng/football-betting-analysis.git football-betting-analysis
   git clone git@github.com:1442334458-eng/football-match-analysis.git football-match-analysis__skillhub
   git clone git@github.com:1442334458-eng/football-pipeline-v8.git football-pipeline-v8
   ```
   > 本地目录名务必保留 `__skillhub` 这段，WorkBuddy 按目录名识别技能。
5. **让 WorkBuddy 读到**：技能已直接住在用户级 `skills` 目录，WorkBuddy 自动识别；重启一次 WorkBuddy。
6. **放 token（备用 / Claw 仓库用）**：`<SKILLS>\ .git_token` 写入你的 GitHub PAT（纯文本一行）。该文件被 `.gitignore` 忽略，不会上传。

---

## 四、把本机公钥加到 GitHub（推送前必做）

1. 打开 https://github.com/settings/ssh/new
2. Title 填 `claw-xieyu`，Key 粘贴下面这串（xieyu 的公钥）：
   ```
   ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAIDcFuJ+A1RE/tGU+LyqM1jJOAy95TVYT8iJoizXHw5pw claw-xieyu@github-sync
   ```
3. 点 **Add SSH key**。
4. 验证：`ssh -T git@github.com` 应回 `Hi 1442334458-eng! You've successfully authenticated...`
5. 然后双击桌面 `GitHub同步.bat` 即可把 3 个仓库推上去。

> Surface 同理：把它的公钥也加进**同一个** GitHub 账号（可 Title 填 `claw-surface`）。

---

## 五、日常同步（只讲这一种）

- **xieyu**：双击桌面 `GitHub同步.bat`。
- **Surface**：运行等价的 `sync_git.bat`（把脚本里的 `SKILLS` 路径改成本机 `C:\Users\Surface\.workbuddy\skills`）。

脚本对每个仓库执行：`git add -A` → 有改动就 `commit` → `git pull origin main`（合并对方改动）→ `git push origin main`。

建议两台都养成习惯：**改完技能就跑一次同步脚本**，对方下次同步即可收到。

---

## 六、冲突怎么办（真合并）

- 大部分情况 git 自动合并成功，无需干预。
- 同一文件同一段两边各改 → 出现 `<<<<<<< HEAD` / `=======` / `>>>>>>>`，打开文件手动取舍、删掉标记后保存，再跑一次同步脚本即可。
- Claw 主仓库（本指南所在）若冲突：`git pull` 后手动解，再 `commit`（post-commit 钩子自动推）。

---

## 七、常见问题

- **`Permission denied (publickey)`**：公钥没加到 GitHub，回第四节。
- **push 卡在 github.com:443**：与本方案无关——已改用 SSH(22) 通道；若仍报 443，检查是否误用了 HTTPS remote（`git remote -v` 应为 `git@github.com:...`）。
- **WorkBuddy 读不到技能**：确认符号链接/目录存在，并**重启 WorkBuddy** 重新扫描技能目录。
- **`Recv failure / Could not connect to github.com:443`**：环境对 443 封锁，正常现象；SSH 通道不受影响，忽略即可。

---

## 八、附：Claw 主仓库本身

本指南、`git-sync.py`、`sync_git.bat` 等文件住在 `D:\1\Claw`（Claw 主仓库）。它的同步与本机 `post-commit` 钩子绑定：本机 `commit` 后钩子自动 `push`（走 HTTPS+token）；另一台 `git pull` 收最新。

> 若 Claw 主仓库的自动推送也受 443 封锁影响，可把它的 remote 也改成 SSH（与 3 个技能仓库同理），并相应调整 `post-commit` 钩子里的推送地址。需要时再说。
