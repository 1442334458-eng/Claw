# 双机 GitHub 同步设置指南（唯一标准做法）

> 老电脑（xieyu）和新电脑（Surface）都严格按本文操作，就能保持一致。
> 本文是「唯一真相源」，改动本文后推送到 GitHub，两台电脑拉取后看到的是同一份说明。

---

## 一、核心思路（两台电脑完全相同）

1. **唯一真相源 = GitHub 仓库 `1442334458-eng/Claw` 里的 `.workbuddy/skills/`。**
   所有足球分析技能都只在这个目录里维护，不分散在别处。
2. **让 WorkBuddy 直接读仓库里的技能**：把 WorkBuddy 的用户级技能目录，
   改成指向仓库技能目录的「目录联接（junction）」。这样 WorkBuddy 看到的技能
   就是仓库里的技能，且随 git 同步自动更新，无需手动复制。
3. **同步动作 = 双击桌面 `GitHub同步.bat`**（脚本会先 pull 远程、再 push 本地，
   并输出「成功/失败文件数」报告）。

---

## ⚠️ 重要：双机同步只用 `sync.sh`，不要用 `git-sync.py`

仓库 `.workbuddy/skills/` 里有一份 `git-sync.py`，它是**旧版/另一套机制**：
它会把技能推到 **3 个独立的 GitHub 仓库**（`football-betting-analysis`、
`football-match-analysis`、`football-pipeline-v8`），**不会同步本 Claw 仓库**。

这与「一个 Claw 仓库、两台电脑 GitHub-only 同步」的目标相冲突，**禁止把它用作双机同步工具**。
它当前的 `_load_token()` 也只读 `data-pipeline/.git_token`（不读 `skills/.git_token`）。

> 结论：两台电脑一律用 `sync.sh` + 桌面 `GitHub同步.bat` 同步 Claw 仓库；
> `git-sync.py` 仅作为技能包内的参考文件保留，不参与日常同步。

---

## 二、每台电脑首次设置（4 步，两台都做）

### 第 1 步：确保装了 Git
没装就去 https://git-scm.com 装一个（已装可跳过）。

### 第 2 步：克隆仓库到本地某目录 `<LOCAL>`
- 老电脑 xieyu：`<LOCAL>` = `D:\1\Claw`（已经克隆好，跳过）
- 新电脑 Surface：例如 `<LOCAL>` = `C:\Claw`

```bat
git clone https://github.com/1442334458-eng/Claw.git <LOCAL>
```

### 第 3 步：放入 PAT（Personal Access Token）
在 `<LOCAL>\data-pipeline\.git_token` 写入你的 GitHub token（纯文本，一行）。
此文件已被 `.gitignore` 忽略，**不会上传到 GitHub**，放心放。

### 第 4 步：让 WorkBuddy 直接读仓库里的技能（运行辅助脚本）
仓库里已带 `setup_skills_junction.bat`。**双击运行它即可**（它会把这 4 个足球技能目录
逐个接成指向仓库技能的「目录联接」，并自动把原有目录备份为 `.bak`，不会动到其它技能）。

> 原理：只 junction 这 4 个技能文件夹，用户级 `.workbuddy\skills` 里的其它技能原样保留。
> 运行后**重启 WorkBuddy** 让它重新扫描技能目录。

- 老电脑：双击 `D:\1\Claw\setup_skills_junction.bat`
- 新电脑：克隆后双击 `<LOCAL>\setup_skills_junction.bat`

（如需手动，脚本等价于对 `football-betting-analysis` / `football-match-analysis__skillhub` /
`football-pipeline-v8` / `football-sync` 四个目录分别 `mklink /J 用户级\技能 仓库\.workbuddy\skills\技能`。）

> 验证：执行后打开 `C:\Users\<user>\.workbuddy\skills`，应能看到这 4 个技能文件夹
> （指向仓库里的同名目录）。

---

## 三、创建桌面一键同步（每台各建一个）

桌面 `GitHub同步.bat` 内容（注意两处路径按本机改）：

```bat
@echo off
cd /d <LOCAL>
C:\Users\<user>\.workbuddy\vendor\PortableGit\bin\bash.exe sync.sh
echo.
echo 按任意键关闭...
pause
```

- 老电脑：`<LOCAL>` = `D:\1\Claw`，bash 路径 = `C:\Users\xieyu\.workbuddy\vendor\PortableGit\bin\bash.exe`
- 新电脑：`<LOCAL>` = `C:\Claw`，bash 路径 = `C:\Users\Surface\.workbuddy\vendor\PortableGit\bin\bash.exe`

（仓库里已自带 `sync.sh`，无需另建。）

---

## 四、日常同步

- 想同步时：**双击桌面 `GitHub同步.bat`**，看末尾报告即可。
- 任意一台更新后，另一台双击 bat 就能拉取到。
- 提交由 `sync.sh` 自动完成；另外本仓库装了 `post-commit` 钩子，
  本机每次 commit 后会自动推送（双保险）。

---

## 五、轮换 token（安全操作）

1. GitHub → Settings → Developer settings → Personal access tokens →
   删除那个**曾硬编码在 git-sync.py、被拦截推送暴露的旧 token**（以 `ghp_` 开头，
   长度约 36 位；就是你在另一台电脑截图里看到的那串）。必须轮换。
2. 新建 token（勾选 `repo` 权限）。
3. 在两台电脑分别把 `<LOCAL>\data-pipeline\.git_token` 内容换成新 token。
4. 双击 bat 验证同步正常。

> 注意：Claw 仓库同步用的 PAT（以 `ghp_` 开头、保存在本机 `data-pipeline/.git_token` 里）
> 没进过任何 Git 提交，目前安全，无需轮换。

---

## 六、冲突怎么办

- 若两台同时改了同一个文件，git 会标记冲突（文件里出现 `<<<<<<<` `=======` `>>>>>>>`），
  **不会丢数据**。手动合并后保存，再双击 bat 即可。
- 若 pull 报告「合并冲突，请手动处理」，同样手动改带标记的文件，然后双击 bat。

---

## 七、常见问题

**Q：WorkBuddy 读不到技能？**
A：确认第 4 步的 junction 建成功（`dir C:\Users\<user>\.workbuddy\skills` 能看到仓库里的技能）。
   重启 WorkBuddy 让它重新扫描技能目录。

**Q：双击 bat 提示「bash 不是内部命令」？**
A：bat 里的 bash 路径写错了，按第 3 节本机真实路径改。

**Q：换电脑后技能版本不一致？**
A：以 GitHub 仓库里的 `.workbuddy/skills/` 为准，双击 bat 拉取最新即可。
