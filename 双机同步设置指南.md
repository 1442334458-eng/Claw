# 双机 GitHub 同步设置指南（唯一标准：git-sync.py）

> xieyu 与 Surface 都按本文操作即一致。本文是唯一真相源，改动后推到 GitHub，另一台 `git pull` 即可看到同一份。

---

## 一、核心机制（两台完全相同）

- **技能同步工具 = `git-sync.py`（API 版）**：通过 GitHub REST API，把 4 个足球技能推/拉到 **3 个独立仓库**
  `football-betting-analysis` / `football-match-analysis` / `football-pipeline-v8`
  （`football-sync` 等随附文件作为额外同步项一起处理）。
- **凭证 = 本机技能目录里的 `.git_token`**（纯文本一行 PAT），已被 `.gitignore` 忽略，**不会上传**。
- **WorkBuddy 读取技能 = 目录联接（junction）**：把用户级技能目录指到技能源（见第二节第 4 步），这样 WorkBuddy 读到的就是 GitHub 同步过来的技能。
- **Claw 仓库本身**（本指南、`git-sync.py` 文件）用普通 git 同步：本机 `commit` 后由 `post-commit` 钩子自动推送，另一台 `git pull` 收最新。

---

## 二、每台首次设置（4 步）

### 第 1 步：确保装了 Git + Python
Git：https://git-scm.com ；Python：https://python.org （已装跳过）。

### 第 2 步：确定技能源目录 `<SKILLS>`
- Surface：`<SKILLS>` = `C:\Users\Surface\.workbuddy\skills`（无需克隆 Claw 仓库）
- xieyu：`<SKILLS>` = `D:\1\Claw\.workbuddy\skills`（Claw 仓库已克隆在此）

### 第 3 步：放 token
在 `<SKILLS>\ .git_token` 写入你的 GitHub PAT（纯文本，一行）。
- Surface：`C:\Users\Surface\.workbuddy\skills\.git_token`
- xieyu：`D:\1\Claw\.workbuddy\skills\.git_token`

此文件被 `.gitignore` 忽略，不会上传，放心放。

### 第 4 步：让 WorkBuddy 读到技能（junction）
- xieyu：已运行 `D:\1\Claw\setup_skills_junction.bat`，**重启 WorkBuddy** 生效。
- Surface：若用户级 `skills` 目录本身就是技能源，可跳过；否则用同样的 junction 思路把用户级技能目录接成指向 `<SKILLS>`。

> 验证：打开 `C:\Users\<user>\.workbuddy\skills`，应能看到这 4 个技能文件夹（指向技能源）。

---

## 三、日常同步（只用 GitHub，不用 sync.sh）

在技能源目录 `<SKILLS>` 下执行：

```bat
python3 git-sync.py push    # 把本机技能推到 GitHub（3 个独立仓库）
python3 git-sync.py pull    # 拉取另一台推上来的改动
python3 git-sync.py status  # 比较本地与远程差异
```

- 推送后脚本会提示「另一台需 pull」。
- Claw 仓库里的文件（本指南、`git-sync.py`）：`git pull` 拉取；本机改了 `git commit` 后钩子自动推送，无需手动 push。

---

## 四、轮换 token（安全操作）

1. GitHub → Settings → Developer settings → Personal access tokens →
   删除那个**曾硬编码在 git-sync.py、被拦截推送暴露的旧 token**（另一台截图里那串 `ghp_` 开头、约 36 位）。
2. 新建 token（勾选 `repo` 权限）。
3. 两台分别把 `<SKILLS>\ .git_token` 内容换成新 token。
4. 跑一次 `python3 git-sync.py push` 验证。

---

## 五、冲突怎么办

- **技能文件冲突**：git-sync.py 为覆盖式写入。若两台同时改了同一文件，先 `pull` 的一方会覆盖；
  重要改动请先沟通或手动合并后再 push。
- **Claw 仓库冲突**：手动解 `<<<<<<<` / `=======` / `>>>>>>>` 后 `git commit`。

---

## 六、常见问题

**Q：WorkBuddy 读不到技能？**
A：确认第二节第 4 步的 junction 生效，并**重启 WorkBuddy** 重新扫描技能目录。

**Q：git-sync.py 报 401 / 403？**
A：`.git_token` 没放对位置，或 token 失效/权限不足。按第四节重设（token 需 `repo` 权限）。

**Q：两台技能版本不一致？**
A：以 3 个 GitHub 仓库里的技能为准，在 `<SKILLS>` 下 `python3 git-sync.py pull` 拉最新。
